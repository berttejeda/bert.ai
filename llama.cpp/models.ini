[*]
# Global settings applied to all models
n_ctx = 4096
n_gpu_layers = -1

# Example model alias — update the path to match your local setup
# [my-model-alias]
# model = /path/to/your/models/gemma-4-E2B-it-uncensored-Q8_0/gemma-4-E2B-it-uncensored-Q8_0.gguf
# chat_template = "{% for message in messages %}{{ message['role'] }}: {{ message['content'] }}\n{% endfor %}"
